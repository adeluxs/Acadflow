@extends('layouts.app')
@section('title','Assignment Management')
@section('content')
<div class="mx-auto max-w-7xl space-y-6 px-4 py-8 sm:px-6 lg:px-8">
    <section class="rounded-[2rem] bg-gradient-to-br from-slate-950 via-indigo-950 to-slate-900 p-7 text-white shadow-xl sm:p-9"><div class="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><div><p class="text-xs font-black uppercase tracking-[.24em] text-indigo-300">{{ $course->code }} · Lecturer workspace</p><h1 class="mt-2 text-3xl font-black sm:text-4xl">Assignment management</h1><p class="mt-3 max-w-2xl text-sm leading-7 text-slate-300">Create coursework, control publishing, monitor submissions and move assignments through their lifecycle.</p></div><a href="{{ route('submission-tasks.create',$course) }}" class="rounded-xl bg-white px-5 py-3 text-sm font-black text-slate-950">+ Create assignment</a></div></section>
    <div class="flex flex-col gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div><p class="text-sm font-black text-slate-900">{{ $tasks->count() }} assignment{{ $tasks->count()===1?'':'s' }}</p><p class="text-xs text-slate-500">Filter the management list by publishing state.</p></div><select class="rounded-xl border-slate-300 text-sm font-semibold" onchange="filterStatus(this.value)"><option value="">All statuses</option><option value="draft" @selected(request('status')==='draft')>Draft</option><option value="published" @selected(request('status')==='published')>Published</option><option value="closed" @selected(request('status')==='closed')>Closed</option><option value="archived" @selected(request('status')==='archived')>Archived</option></select></div>
    <div class="grid gap-5 lg:grid-cols-2">@forelse($tasks as $task)<article class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div class="flex items-start justify-between gap-4"><div><p class="text-xs font-black uppercase tracking-wide text-indigo-600">{{ str($task->type)->headline() }}</p><h2 class="mt-1 text-xl font-black text-slate-900">{{ $task->title }}</h2></div><span class="rounded-full px-3 py-1 text-xs font-bold {{ $task->status==='published'?'bg-emerald-100 text-emerald-700':($task->status==='draft'?'bg-amber-100 text-amber-700':($task->status==='closed'?'bg-rose-100 text-rose-700':'bg-slate-100 text-slate-600')) }}">{{ str($task->status)->headline() }}</span></div><p class="mt-3 line-clamp-2 text-sm leading-6 text-slate-600">{{ \Str::limit($task->description,140) }}</p><div class="mt-5 grid grid-cols-2 gap-3"><div class="rounded-2xl bg-slate-50 p-4"><p class="text-[10px] font-black uppercase text-slate-400">Deadline</p><p class="mt-1 text-sm font-bold text-slate-800">{{ $task->due_date?->format('M j, Y · H:i') ?? 'No deadline' }}</p>@if($task->due_date && $task->due_date<now())<p class="mt-1 text-xs font-bold text-rose-600">Overdue</p>@endif</div><div class="rounded-2xl bg-slate-50 p-4"><p class="text-[10px] font-black uppercase text-slate-400">Submissions</p><p class="mt-1 text-2xl font-black text-slate-900">{{ $task->submissions_count ?? 0 }}</p></div></div><div class="mt-5 flex flex-wrap gap-2 border-t border-slate-100 pt-4"><a href="{{ route('submission-tasks.lecturer.show',[$course,$task]) }}" class="rounded-xl bg-slate-900 px-4 py-2 text-sm font-black text-white">Open</a>@if($task->status==='draft')<a href="{{ route('submission-tasks.edit',[$course,$task]) }}" class="rounded-xl border border-slate-200 px-4 py-2 text-sm font-bold text-slate-700">Edit</a><form action="{{ route('submission-tasks.publish',[$course,$task]) }}" method="POST">@csrf<button onclick="return confirm('Publish this assignment?')" class="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-bold text-white">Publish</button></form><form action="{{ route('submission-tasks.destroy',[$course,$task]) }}" method="POST">@csrf @method('DELETE')<button onclick="return confirm('Delete this assignment?')" class="rounded-xl border border-rose-200 px-4 py-2 text-sm font-bold text-rose-700">Delete</button></form>@elseif($task->status==='published')<form action="{{ route('submission-tasks.close',[$course,$task]) }}" method="POST">@csrf<button onclick="return confirm('Close this assignment?')" class="rounded-xl bg-amber-600 px-4 py-2 text-sm font-bold text-white">Close</button></form>@endif</div></article>@empty<div class="lg:col-span-2 rounded-[2rem] border border-dashed border-slate-300 bg-white p-14 text-center"><h2 class="text-xl font-black text-slate-900">No assignments yet</h2><p class="mt-2 text-sm text-slate-500">Create the first assignment for this course.</p><a href="{{ route('submission-tasks.create',$course) }}" class="mt-5 inline-flex rounded-xl bg-indigo-600 px-5 py-2.5 text-sm font-bold text-white">Create assignment</a></div>@endforelse</div>
</div>
<script>function filterStatus(status){const url=new URL(window.location);status?url.searchParams.set('status',status):url.searchParams.delete('status');window.location=url;}</script>
@endsection
